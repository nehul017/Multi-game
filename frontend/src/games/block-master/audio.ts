export type BlockMasterCue = 'move' | 'rotate' | 'lock' | 'clear' | 'drop' | 'over' | 'level';

/** Placeholder for a future shared game-audio service. */
export function playBlockMasterCue(_cue: BlockMasterCue) {
  return;
}
